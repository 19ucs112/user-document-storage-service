spring:
  application:
    name: user-document-storage-service
com:
  user:
    data:
      storage:
        aws:
          s3:
            s3bucket-name: test
            s3-storage-prefix: /documents
          aws-region: ap-south-1
          aws-secret-key: test
          aws-access-key: test